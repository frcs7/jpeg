#include <stdio.h>

#include <math.h>

#include <iostream>

#include <vector>

#include "DCT.h"

using namespace std;

#pragma warning(disable : 4996)；

/*

string binary(int n)

{

    string r;

    while(n != 0)

    {

        r += (n%2 == 0 ? "0" : "1");

        n/2;

    }

    return r;

}

short codelength(int a) //a=ZY[k][i]

{

    short m=0;

    do

    {
        m++;
    } while (a >= pow(2, m));

    return m;

}



do

{
    m++;
} while (i < pow(2, m));

int temp = 16 - ZY[k][i];

string bi;

bi = binary(temp);

RLEtemp.push_back(counter);

RLEtemp.push_back(m);

RLEtemp.push_back(bi);


*/
/*
double *tomatrix(double X[256 * 256])
{

    // double matrixX[256][256];

    double (*r)[256] = new double[256][256];

    for (int i = 0; i < 256; i++)

    {

        for (int j = 0; j < 256; j++)

        {

            r[i][j] = X[256 * i + j] - 128; // Y数据的矩阵
        }
    }

}*/
int main(){

int a=3, *p;
*p=4;
cout<<*p<<endl;

return 0;
}

